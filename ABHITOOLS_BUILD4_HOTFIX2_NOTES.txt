ABHITOOLS DESIGN BUILD 4 HOTFIX 2
Date: 2026-08-24

FIX 1 — LOAN AMOUNT PRESENTATION
- Loan summary cards now use compact Indian-friendly display:
  ₹1,000 -> ₹1K
  ₹25,000 -> ₹25K
  ₹1,00,000 -> ₹1L
  ₹1,25,000 -> ₹1.25L
  ₹1,00,00,000 -> ₹1Cr
- Full exact Indian-format amount remains available in the title/tooltip and all detailed financial screens.
- EMI/payment calculations are unchanged.
- Overview, statements, receipts and ledger logic still use exact values.

FIX 2 — ADMIN PANEL SHOWING BEFORE LOGIN
- Service worker now performs auth-first navigation for /admin.html.
- It checks /api/auth before allowing admin HTML navigation.
- 401/auth failure redirects to advanced_admin_login_panel.html before the controlled admin page renders.
- This hotfix service worker activates immediately so the security/UI fix takes effect without waiting for an old worker lifecycle.
- Existing signed-session behavior remains unchanged: a genuinely valid admin session is still accepted.

SAFETY
- No API/serverless files changed.
- No Supabase/database change.
- No session credential/token change.
- No EMI date/status logic changed.
- /api/* remains network-only and is never cached.
- Vercel function count remains unchanged.
