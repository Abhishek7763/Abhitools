ABHITOOLS DESIGN BUILD 4 — FORMS + SECONDARY SCREENS
Date: 2026-08-24

SCOPE:
Responsive/mobile-first polish only. No financial/business calculations or backend routes changed.

CHANGES:
1. Add Borrower form
   - Full-height mobile form, compact desktop card.
   - One-column mobile, two-column desktop.
   - Sticky Save/Cancel actions and top close button.

2. Add/Edit Loan form
   - Full-height mobile form.
   - Compact EMI editor; no page-level horizontal scroll.
   - One-column core fields on mobile, three columns on desktop.
   - Sticky Save/Cancel actions.

3. Payment / Follow-up / WhatsApp / Settlement forms
   - Mobile full-screen dialog behavior.
   - Compact responsive fields and touch targets.
   - Sticky long-form action areas where existing structure supports it.

4. Secondary admin screens
   - Borrower Profile, Due, Search, Reports, Data Quality, Activity, Settings, Backup/Restore,
     Collection Insights, Calendar, Release/Recovery and related modals use a common responsive shell.
   - Sticky headers on long screens.
   - Tables scroll inside their own wrapper instead of forcing page horizontal scroll.
   - Filter chip/period rows remain horizontally scrollable within the control only.

5. Navigation cleanup
   - Old Phase-15 mobile quick navigation is hidden because Build-1 bottom navigation is authoritative.

6. Accessibility/usability
   - Close buttons/EMI editor controls receive aria labels when missing.
   - Escape closes the top visible standard modal/form when safe.
   - Visible keyboard focus and practical mobile control heights.

SAFETY:
- No api/* files changed.
- No Supabase/database migration.
- No EMI due date/status calculation change.
- No payment/settlement/PTP business logic change.
- No Vercel function added; budget remains 12.
- Hotfix auth gate and deterministic UI script loading are preserved.
- /api/* remains network-only in service worker.

FILES TO UPLOAD/REPLACE IN REPO ROOT:
- pwa.js
- service-worker.js
- ui_forms_secondary.css (new)
- ui_forms_secondary.js (new)
- ABHITOOLS_DESIGN_BUILD4_NOTES.txt (optional)
- BUILD4_MANIFEST.json (optional)

NEXT AFTER LIVE VERIFICATION:
Design Build 5 — Print/PDF + final responsive/accessibility regression + final stable release polish.
